# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# OS
.DS_Store
Thumbs.db

# Runtime / logs / scratch
logs/
*.log
*.tmp
*.json.tmp

# Local IPC / device artifacts (never committed)
ipc/
share/status.json
share/control.json

# the unpacked Csound bundle — the tracked artefact is the tarball beside it
move/bundle/csound/

# the unpacked CDP set — the tracked artefact is the tarball beside it
move/bundle/cdp/

# Built Armbian bundle (hundreds of MB — distributed as a GitHub release asset)
# Root-anchored: move/bundle/ holds the vendored runtime tarballs and IS tracked.
/bundle/
