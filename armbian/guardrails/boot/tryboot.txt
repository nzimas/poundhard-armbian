# ARMBIAN — reached ONLY through the firmware's one-shot tryboot (stock's ph-hop).
# A normal power-on reads config.txt, which is stock. Any Armbian failure => stock.
arm_64bit=1
enable_uart=1
over_voltage=4
kernel=armbian-Image
device_tree=bcm2711-rpi-cm4-armbian.dtb
cmdline=armbian-cmdline.txt
dtparam=spi=on
dtoverlay=dwc2,dr_mode=otg
dtoverlay=ablspi-move-cm4
dtoverlay=move-spidev0-off
